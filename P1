PROMPT 1 - AUDIT ONLY

You are working in this repository. Do not make any code changes for this task.

Scan the entire codebase and identify which UI components and logic modules are already implemented as shared, reusable code, versus which patterns are duplicated across multiple files with separate, inconsistent implementations of essentially the same thing. Include UI elements such as buttons, scrollbars, tooltips or popovers, dropdowns, modals, cards, and any console or live-updating views, as well as backend or logic patterns such as data fetching, polling, status checks, filtering, and notification handling. This list is not exhaustive, include anything else you find repeated.

For each pattern that appears in more than one place, list every file and location where it appears, note which implementation looks like the intended or most complete version, and note specifically how the other implementations differ from or fall short of it.

Also list anything that is already properly centralized as a shared module today, so I know what exists and where it lives.

Base this purely on what you find in the codebase and on any conventions or documentation already present in this repository. Do not assume or invent an intended design.

Be thorough, check the whole codebase rather than only recently changed files.

Present this entirely as a report. Do not refactor, rename, move, or edit any files in this pass.


PROMPT 2 - CENTRALIZE

Based on the audit you produced, propose a shared module structure to hold one canonical implementation of each duplicated pattern you identified. Show me that proposed structure before changing anything.

Once I approve it, refactor every existing usage across the codebase to reference the shared implementation instead of its local, duplicated copy, without changing existing behavior.

Create or update a central document that lists every shared component or module, what it does, where it lives, and how to use it.

Update whichever conventions or instructions file this repository already uses for coding agents, so that going forward, before creating any new UI element or common piece of logic, the agent checks that document first and reuses or extends an existing shared implementation instead of creating a new one-off. Only add something new to the shared modules if nothing already covers the need, and document it there when you do.

Work through this in stages so I can review the proposed structure before you start editing files across the codebase.
