Create one reusable DropdownMenu component using shadcn/ui's DropdownMenu (built on Radix UI), and use it for every dropdown/menu in the app — no one-off custom dropdowns anywhere. Each item should show an icon (lucide-react) plus a label. On hover, highlight the row with a background color change; for a currently selected option, show it with a background highlight or checkmark. Use Radix's built-in open/closed state attributes with Tailwind's animate-in/animate-out utilities for smooth open and close transitions. Style it properly for both light and dark mode using our existing dark mode setup.
