Visual polish pass on the report — keep the current background color, the pill progress bar colors, and the pie chart slice color palette exactly as they are. Only change the following:
	1.	Header title color — change the report title from bright purple to a muted, professional color (e.g., dark navy or slate), consistent with a corporate dashboard rather than a bright accent color.
	2.	Card top-border colors — each namespace card currently has a different bright accent color across its top edge (pink, orange, teal, green, blue, purple), which reads as too colorful. Replace with one consistent muted accent color across all cards, or remove the colored top border entirely and rely on the card’s shadow/rounded corners alone for definition.
	3.	Pie chart hover effect — tone down significantly. The current hover “pull” on pie slices is too large, causing excessive slice separation, and it’s incorrectly shifting the chart’s title position on hover — that should never happen. Reduce the pull distance to something subtle, and make sure the chart title and overall layout are fixed and unaffected by hover state (a restyle on hover should not trigger any relayout of surrounding elements). Favor a slightly brighter/highlighted border on the hovered slice as the primary emphasis rather than a large pull distance, and make the hover transition itself smoother and slower rather than the current sharp, sudden movement.
	4.	Tooltip style consistency. The namespace card tooltips (the per-deployment breakdown shown on hover) use a dark background, white text, and rounded corners — that’s the style to standardize on everywhere. The pie chart’s hover tooltips currently render as a plain white/cream box with square corners — restyle them to match the dark, rounded tooltip style used elsewhere.
	5.	Legend panel styling. The legend next to the pie charts currently blends directly into the surrounding card with no visual separation and square corners. Give it its own distinct container — a slightly different background shade from the card behind it, with rounded corners matching the rest of the report’s cards.
	6.	Pie chart subtitle weight. Titles like “CPU: Reserved (Limit) Share of Total Quota” are currently regular weight — make these bold for better visual hierarchy.
