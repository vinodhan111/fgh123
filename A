In the user management section of the application, replace the manual password entry field with an auto generated password system. When creating a new user, the admin should only input a username. On save, the backend should generate a random password of length ten using Python's built in secrets module, not the standard random module, since secrets is cryptographically secure. The password should be built from lowercase letters, uppercase letters, digits, and a safe set of special characters, excluding visually ambiguous characters like uppercase O, zero, uppercase I, lowercase L, and one. Use secrets.choice to guarantee at least one character from each of the four categories, then fill the remaining characters randomly from the combined pool using secrets.choice, and use secrets.SystemRandom to shuffle the final result. Hash the generated password using the existing salt and hash logic before storing it, and return the plaintext password in the create user API response exactly once. On the frontend, show the returned plaintext password in a dialog with a copy to clipboard button, and never fetch or display it again after that dialog is closed. Apply the same generation and one time display logic to the existing reset password flow, so resetting a password overwrites the stored hash and returns a new plaintext password shown the same way. End quote.
